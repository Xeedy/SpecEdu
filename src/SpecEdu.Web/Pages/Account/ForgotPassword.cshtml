@page
@model SpecEdu.Web.Pages.Account.ForgotPasswordModel
@{
    ViewData["Title"] = "Zapomenute heslo";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-5">

            <div class="text-center mb-4">
                <h1 class="mb-2">Zapomenute heslo</h1>
                <p class="text-muted">Zadejte svuj e-mail a my vam zaslem odkaz pro obnoveni hesla</p>
            </div>

            @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
            {
                <div class="alert alert-danger" role="alert">
                    @Model.ErrorMessage
                </div>
            }

            <div class="spec-card">
                <div class="spec-card-body">
                    <form method="post" class="needs-validation" novalidate>
                        <div class="mb-3">
                            <label asp-for="Input.Email" class="form-label"></label>
                            <input type="email" class="form-control" asp-for="Input.Email"
                                   placeholder="vas@email.cz" required autofocus />
                            <span class="text-danger" asp-validation-for="Input.Email"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-spec-primary">
                                <i class="bi bi-envelope me-2"></i>Odeslat odkaz
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="text-center mt-3">
                <a asp-page="/Account/Login" class="text-muted small">
                    <i class="bi bi-arrow-left me-1"></i>Zpet na prihlaseni
                </a>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        (function () {
            const forms = document.querySelectorAll('.needs-validation');
            Array.prototype.slice.call(forms).forEach(function (form) {
                form.addEventListener('submit', function (event) {
                    if (!form.checkValidity()) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        })();
    </script>
}
